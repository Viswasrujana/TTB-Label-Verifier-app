# Used by both Docker and Streamlit Cloud
streamlit==1.36.0
httpx==0.27.0
pydantic==2.7.4
Pillow==10.4.0
pandas==2.2.2
python-dotenv==1.0.1

# Needed for the FastAPI backend (Docker only — Streamlit Cloud ignores these)
fastapi==0.111.1
uvicorn[standard]==0.30.1
python-multipart==0.0.9
